@model IEnumerable<MCCWebMVC.Models.Karyawan>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ktp)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Telpon)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Jabatan)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Gaji)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ktp)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telpon)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.JabatanId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GajiId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
